mv .github/workflows/ci.yml .github/workflows/main.yml
echo "name: CI FastAPI Backend
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: 3.12
    - run: pip install -r requirements-azure.txt pytest
    - run: pytest tests/ -v" > .github/workflows/main.yml
